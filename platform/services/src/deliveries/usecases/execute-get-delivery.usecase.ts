/**
 * ExecuteGetDelivery - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deliveries
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetDelivery Use Case
 *
 * DDD: Application use case for get Delivery.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetDeliveryInput, GetDeliveryOutput } from "../dto/delivery.dto";
import type { ExecutionContextService, IdGeneratorService } from "@narriva/services/_shared/index.js";
import type { DeliveryRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetDelivery {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly delivery: DeliveryRepository
  ) {}

  async execute(input: GetDeliveryInput): Promise<GetDeliveryOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.dlvId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).deliveryId) {
      throw new ValidationError("deliveryId is required");
    }
    const deliveryId = (input as any).deliveryId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.delivery.getDelivery({ deliveryId: deliveryId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${deliveryId}`);
    }

    return entity as GetDeliveryOutput;
  }
}
