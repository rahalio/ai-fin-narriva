/**
 * ExecuteListDisclosureRules - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: templates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteListDisclosureRules Use Case
 *
 * DDD: Application use case for list DisclosureRule.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ListDisclosureRulesInput, ListDisclosureRulesOutput } from "../dto/disclosure-rule.dto";
import type { ExecutionContextService, IdGeneratorService } from "@narriva/services/_shared/index.js";
import type { DisclosureRuleRepository } from "../ports";

export class ExecuteListDisclosureRules {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly disclosureRule: DisclosureRuleRepository
  ) {}

  async execute(input: ListDisclosureRulesInput): Promise<ListDisclosureRulesOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.tplId();

    // All field names extracted from OpenAPI spec: none
    // List entities via repository
    const result = await this.disclosureRule.listDisclosureRules({ ...input, orgId: this.context.getOrgId(), correlationId } as any);

    return result as ListDisclosureRulesOutput;
  }
}
