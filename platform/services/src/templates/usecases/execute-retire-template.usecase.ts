/**
 * ExecuteRetireTemplate - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: templates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteRetireTemplate Use Case
 *
 * DDD: Application use case for get Retire.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { RetireTemplateInput, RetireTemplateOutput } from "../dto/retire.dto";
import type { ExecutionContextService, IdGeneratorService } from "@narriva/services/_shared/index.js";
import type { RetireRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteRetireTemplate {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly retire: RetireRepository
  ) {}

  async execute(input: RetireTemplateInput): Promise<RetireTemplateOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.tplId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).templateId) {
      throw new ValidationError("templateId is required");
    }
    const templateId = (input as any).templateId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.retire.retireTemplate({ templateId: templateId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${templateId}`);
    }

    return entity as RetireTemplateOutput;
  }
}
