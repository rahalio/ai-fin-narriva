/**
 * Narratives Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: narratives
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const generateNarrative_Body = z
  .object({
    snapshotId: z.string(),
    templateId: z.string(),
    locale: z.string().optional(),
  })
  .passthrough();

export const schemas: any = {
  generateNarrative_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v0/tenants/me/narratives',
    alias: 'listNarratives',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'periodKey',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z
          .enum([
            'generated',
            'failed_missing_facts',
            'pending_review',
            'approved',
            'rejected',
            'delivered',
          ])
          .optional(),
      },
      {
        name: 'audience',
        type: 'Query',
        schema: z
          .enum(['retail_client', 'advisor', 'institutional'])
          .optional(),
      },
      {
        name: 'snapshotId',
        type: 'Query',
        schema: z.string().optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  narrativeId: z.string().regex(/^nar_[0-9A-HJKMNP-TV-Z]{26}$/),
                  snapshotId: z.string(),
                  templateId: z.string(),
                  templateVersion: z.number().int(),
                  periodKey: z.string(),
                  audience: z.enum([
                    'retail_client',
                    'advisor',
                    'institutional',
                  ]),
                  locale: z.string(),
                  text: z.string().optional(),
                  claimBindings: z.array(
                    z
                      .object({
                        claimBindingId: z
                          .string()
                          .regex(/^clm_[0-9A-HJKMNP-TV-Z]{26}$/),
                        sentenceIndex: z.number().int().gte(0),
                        claimText: z.string(),
                        factFieldKey: z.string(),
                        snapshotId: z.string(),
                        locale: z.string().optional(),
                      })
                      .passthrough()
                  ),
                  missingFacts: z.array(z.string()),
                  judgmentOverlay: z.string().optional(),
                  status: z.enum([
                    'generated',
                    'failed_missing_facts',
                    'pending_review',
                    'approved',
                    'rejected',
                    'delivered',
                  ]),
                  reviewGate: z
                    .enum(['auto_send', 'pm_required', 'compliance_required'])
                    .optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/tenants/me/narratives',
    alias: 'generateNarrative',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: generateNarrative_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            narrativeId: z.string().regex(/^nar_[0-9A-HJKMNP-TV-Z]{26}$/),
            snapshotId: z.string(),
            templateId: z.string(),
            templateVersion: z.number().int(),
            periodKey: z.string(),
            audience: z.enum(['retail_client', 'advisor', 'institutional']),
            locale: z.string(),
            text: z.string().optional(),
            claimBindings: z.array(
              z
                .object({
                  claimBindingId: z
                    .string()
                    .regex(/^clm_[0-9A-HJKMNP-TV-Z]{26}$/),
                  sentenceIndex: z.number().int().gte(0),
                  claimText: z.string(),
                  factFieldKey: z.string(),
                  snapshotId: z.string(),
                  locale: z.string().optional(),
                })
                .passthrough()
            ),
            missingFacts: z.array(z.string()),
            judgmentOverlay: z.string().optional(),
            status: z.enum([
              'generated',
              'failed_missing_facts',
              'pending_review',
              'approved',
              'rejected',
              'delivered',
            ]),
            reviewGate: z
              .enum(['auto_send', 'pm_required', 'compliance_required'])
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 422,
        description: `Semantically invalid request (e.g. PACK_EMPTY)`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v0/tenants/me/narratives/:narrativeId',
    alias: 'getNarrative',
    requestFormat: 'json',
    parameters: [
      {
        name: 'narrativeId',
        type: 'Path',
        schema: z.string().regex(/^nar_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            narrativeId: z.string().regex(/^nar_[0-9A-HJKMNP-TV-Z]{26}$/),
            snapshotId: z.string(),
            templateId: z.string(),
            templateVersion: z.number().int(),
            periodKey: z.string(),
            audience: z.enum(['retail_client', 'advisor', 'institutional']),
            locale: z.string(),
            text: z.string().optional(),
            claimBindings: z.array(
              z
                .object({
                  claimBindingId: z
                    .string()
                    .regex(/^clm_[0-9A-HJKMNP-TV-Z]{26}$/),
                  sentenceIndex: z.number().int().gte(0),
                  claimText: z.string(),
                  factFieldKey: z.string(),
                  snapshotId: z.string(),
                  locale: z.string().optional(),
                })
                .passthrough()
            ),
            missingFacts: z.array(z.string()),
            judgmentOverlay: z.string().optional(),
            status: z.enum([
              'generated',
              'failed_missing_facts',
              'pending_review',
              'approved',
              'rejected',
              'delivered',
            ]),
            reviewGate: z
              .enum(['auto_send', 'pm_required', 'compliance_required'])
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/tenants/me/narratives/:narrativeId/judgment',
    alias: 'addNarrativeJudgmentOverlay',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: z
          .object({ judgmentOverlay: z.string().max(8000) })
          .passthrough(),
      },
      {
        name: 'narrativeId',
        type: 'Path',
        schema: z.string().regex(/^nar_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            narrativeId: z.string().regex(/^nar_[0-9A-HJKMNP-TV-Z]{26}$/),
            snapshotId: z.string(),
            templateId: z.string(),
            templateVersion: z.number().int(),
            periodKey: z.string(),
            audience: z.enum(['retail_client', 'advisor', 'institutional']),
            locale: z.string(),
            text: z.string().optional(),
            claimBindings: z.array(
              z
                .object({
                  claimBindingId: z
                    .string()
                    .regex(/^clm_[0-9A-HJKMNP-TV-Z]{26}$/),
                  sentenceIndex: z.number().int().gte(0),
                  claimText: z.string(),
                  factFieldKey: z.string(),
                  snapshotId: z.string(),
                  locale: z.string().optional(),
                })
                .passthrough()
            ),
            missingFacts: z.array(z.string()),
            judgmentOverlay: z.string().optional(),
            status: z.enum([
              'generated',
              'failed_missing_facts',
              'pending_review',
              'approved',
              'rejected',
              'delivered',
            ]),
            reviewGate: z
              .enum(['auto_send', 'pm_required', 'compliance_required'])
              .optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v0/tenants/me/narratives/productivity',
    alias: 'getNarrativeProductivitySummary',
    requestFormat: 'json',
    parameters: [
      {
        name: 'periodKey',
        type: 'Query',
        schema: z.string(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            periodKey: z.string(),
            acceptedCount: z.number().int().gte(0),
            rejectedCount: z.number().int().gte(0),
            hoursSavedAcceptedOnly: z.number(),
            missingFactRate: z.number(),
            claimBindingCoverage: z.number(),
            rejectReasons: z
              .array(
                z
                  .object({ reason: z.string(), count: z.number().int() })
                  .passthrough()
              )
              .optional(),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios('https://api.narriva.local', endpoints);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
