/**
 * Snapshots Zod Schemas - Runtime validation schemas
 *
 * Generated by Zod Schemas Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: snapshots
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

import { makeApi, Zodios, type ZodiosOptions } from '@zodios/core';
import { z } from 'zod';

const createFactSnapshot_Body = z
  .object({
    accountId: z.string().min(1),
    asOf: z.string().datetime({ offset: true }),
    periodKey: z.string().min(1).max(32),
    fields: z
      .array(
        z
          .object({
            key: z.string(),
            value: z.unknown(),
            sourceSystem: z.string(),
            authoritative: z.boolean().optional().default(false),
            requiredForGeneration: z.boolean().optional().default(false),
          })
          .passthrough()
      )
      .min(1),
  })
  .passthrough();

export const schemas: any = {
  createFactSnapshot_Body,
};

const endpoints = makeApi([
  {
    method: 'get',
    path: '/v0/tenants/me/snapshots',
    alias: 'listFactSnapshots',
    requestFormat: 'json',
    parameters: [
      {
        name: 'cursor',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'limit',
        type: 'Query',
        schema: z.number().int().gte(1).lte(200).optional().default(50),
      },
      {
        name: 'periodKey',
        type: 'Query',
        schema: z.string().optional(),
      },
      {
        name: 'status',
        type: 'Query',
        schema: z.enum(['open', 'locked']).optional(),
      },
      {
        name: 'accountId',
        type: 'Query',
        schema: z.string().optional(),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            items: z.array(
              z
                .object({
                  snapshotId: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
                  accountId: z.string().min(1),
                  asOf: z.string().datetime({ offset: true }),
                  periodKey: z.string().min(1).max(32),
                  status: z.enum(['open', 'locked']),
                  fields: z.array(
                    z
                      .object({
                        fieldId: z
                          .string()
                          .regex(/^fld_[0-9A-HJKMNP-TV-Z]{26}$/),
                        key: z.string().min(1).max(128),
                        value: z.unknown(),
                        sourceSystem: z.string().min(1).max(64),
                        authoritative: z.boolean(),
                        requiredForGeneration: z
                          .boolean()
                          .optional()
                          .default(false),
                      })
                      .passthrough()
                  ),
                  missingRequiredKeys: z.array(z.string()),
                  lockedAt: z.string().datetime({ offset: true }).optional(),
                  lockedBy: z.string().optional(),
                  createdAt: z.string().datetime({ offset: true }),
                  updatedAt: z.string().datetime({ offset: true }),
                })
                .passthrough()
            ),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/tenants/me/snapshots',
    alias: 'createFactSnapshot',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: createFactSnapshot_Body,
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            snapshotId: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
            accountId: z.string().min(1),
            asOf: z.string().datetime({ offset: true }),
            periodKey: z.string().min(1).max(32),
            status: z.enum(['open', 'locked']),
            fields: z.array(
              z
                .object({
                  fieldId: z.string().regex(/^fld_[0-9A-HJKMNP-TV-Z]{26}$/),
                  key: z.string().min(1).max(128),
                  value: z.unknown(),
                  sourceSystem: z.string().min(1).max(64),
                  authoritative: z.boolean(),
                  requiredForGeneration: z.boolean().optional().default(false),
                })
                .passthrough()
            ),
            missingRequiredKeys: z.array(z.string()),
            lockedAt: z.string().datetime({ offset: true }).optional(),
            lockedBy: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 409,
        description: `Idempotency key reuse with different body, or state conflict`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'get',
    path: '/v0/tenants/me/snapshots/:snapshotId',
    alias: 'getFactSnapshot',
    requestFormat: 'json',
    parameters: [
      {
        name: 'snapshotId',
        type: 'Path',
        schema: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            snapshotId: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
            accountId: z.string().min(1),
            asOf: z.string().datetime({ offset: true }),
            periodKey: z.string().min(1).max(32),
            status: z.enum(['open', 'locked']),
            fields: z.array(
              z
                .object({
                  fieldId: z.string().regex(/^fld_[0-9A-HJKMNP-TV-Z]{26}$/),
                  key: z.string().min(1).max(128),
                  value: z.unknown(),
                  sourceSystem: z.string().min(1).max(64),
                  authoritative: z.boolean(),
                  requiredForGeneration: z.boolean().optional().default(false),
                })
                .passthrough()
            ),
            missingRequiredKeys: z.array(z.string()),
            lockedAt: z.string().datetime({ offset: true }).optional(),
            lockedBy: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/tenants/me/snapshots/:snapshotId/fields/:fieldId/authoritative',
    alias: 'markFactFieldAuthoritative',
    requestFormat: 'json',
    parameters: [
      {
        name: 'body',
        type: 'Body',
        schema: z.object({ authoritative: z.boolean() }).passthrough(),
      },
      {
        name: 'snapshotId',
        type: 'Path',
        schema: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'fieldId',
        type: 'Path',
        schema: z.string().regex(/^fld_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            snapshotId: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
            accountId: z.string().min(1),
            asOf: z.string().datetime({ offset: true }),
            periodKey: z.string().min(1).max(32),
            status: z.enum(['open', 'locked']),
            fields: z.array(
              z
                .object({
                  fieldId: z.string().regex(/^fld_[0-9A-HJKMNP-TV-Z]{26}$/),
                  key: z.string().min(1).max(128),
                  value: z.unknown(),
                  sourceSystem: z.string().min(1).max(64),
                  authoritative: z.boolean(),
                  requiredForGeneration: z.boolean().optional().default(false),
                })
                .passthrough()
            ),
            missingRequiredKeys: z.array(z.string()),
            lockedAt: z.string().datetime({ offset: true }).optional(),
            lockedBy: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
  {
    method: 'post',
    path: '/v0/tenants/me/snapshots/:snapshotId/lock',
    alias: 'lockFactSnapshot',
    requestFormat: 'json',
    parameters: [
      {
        name: 'snapshotId',
        type: 'Path',
        schema: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
      },
      {
        name: 'Idempotency-Key',
        type: 'Header',
        schema: z.string().min(1).max(128),
      },
    ],
    response: z
      .object({
        data: z
          .object({
            snapshotId: z.string().regex(/^snp_[0-9A-HJKMNP-TV-Z]{26}$/),
            accountId: z.string().min(1),
            asOf: z.string().datetime({ offset: true }),
            periodKey: z.string().min(1).max(32),
            status: z.enum(['open', 'locked']),
            fields: z.array(
              z
                .object({
                  fieldId: z.string().regex(/^fld_[0-9A-HJKMNP-TV-Z]{26}$/),
                  key: z.string().min(1).max(128),
                  value: z.unknown(),
                  sourceSystem: z.string().min(1).max(64),
                  authoritative: z.boolean(),
                  requiredForGeneration: z.boolean().optional().default(false),
                })
                .passthrough()
            ),
            missingRequiredKeys: z.array(z.string()),
            lockedAt: z.string().datetime({ offset: true }).optional(),
            lockedBy: z.string().optional(),
            createdAt: z.string().datetime({ offset: true }),
            updatedAt: z.string().datetime({ offset: true }),
          })
          .passthrough(),
        meta: z
          .object({
            requestId: z.string().uuid(),
            correlationId: z.string(),
            generatedAt: z.string().datetime({ offset: true }),
          })
          .partial()
          .passthrough()
          .optional(),
      })
      .passthrough(),
    errors: [
      {
        status: 400,
        description: `Malformed request`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 401,
        description: `Missing or invalid API key`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 404,
        description: `Resource not found`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
      {
        status: 409,
        description: `Idempotency key reuse with different body, or state conflict`,
        schema: z
          .object({
            type: z.string().url(),
            title: z.string(),
            status: z.number().int(),
            detail: z.string(),
            instance: z.string().url(),
            code: z.string(),
          })
          .partial()
          .passthrough(),
      },
    ],
  },
]);

export const api: any = new Zodios('https://api.narriva.local', endpoints);

export function createApiClient(baseUrl: string, options?: ZodiosOptions): any {
  return new Zodios(baseUrl, endpoints, options);
}
