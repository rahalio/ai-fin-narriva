/**
 * GetFactSnapshot handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: snapshots
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getFactSnapshot } from "@narriva/api-server/domains/snapshots/handlers";
import type { SnapshotsDDDDependencies } from "@narriva/api-server/domains/snapshots";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@narriva/tests/shared";
import { SNAPSHOTS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetFactSnapshot", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SnapshotsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SnapshotsDDDDependencies>(
      SNAPSHOTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetFactSnapshot, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { snapshotId: entity.id };

    await getFactSnapshot(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetFactSnapshot.execute).toHaveBeenCalledWith(expect.objectContaining({ snapshotId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { snapshotId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetFactSnapshot, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getFactSnapshot(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
