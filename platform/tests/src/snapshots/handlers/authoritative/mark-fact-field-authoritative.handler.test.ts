/**
 * MarkFactFieldAuthoritative handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: snapshots
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { markFactFieldAuthoritative } from "@narriva/api-server/domains/snapshots/handlers";
import type { SnapshotsDDDDependencies } from "@narriva/api-server/domains/snapshots";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@narriva/tests/shared";
import { SNAPSHOTS_USE_CASES } from "../__factories__/use-cases.js";

describe("MarkFactFieldAuthoritative", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SnapshotsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SnapshotsDDDDependencies>(
      SNAPSHOTS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeMarkFactFieldAuthoritative, "execute").mockResolvedValue(useCaseResult);

    await markFactFieldAuthoritative(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeMarkFactFieldAuthoritative.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
