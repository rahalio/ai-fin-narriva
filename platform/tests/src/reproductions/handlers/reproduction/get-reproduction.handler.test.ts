/**
 * GetReproduction handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: reproductions
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getReproduction } from "@narriva/api-server/domains/reproductions/handlers";
import type { ReproductionsDDDDependencies } from "@narriva/api-server/domains/reproductions";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@narriva/tests/shared";
import { REPRODUCTIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetReproduction", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ReproductionsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ReproductionsDDDDependencies>(
      REPRODUCTIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetReproduction, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { reproductionId: entity.id };

    await getReproduction(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetReproduction.execute).toHaveBeenCalledWith(expect.objectContaining({ reproductionId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { reproductionId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetReproduction, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getReproduction(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
