/**
 * GetApproval handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: approvals
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getApproval } from "@narriva/api-server/domains/approvals/handlers";
import type { ApprovalsDDDDependencies } from "@narriva/api-server/domains/approvals";
import { createApproval as createApprovalEntity } from "@narriva/factories/approvals";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@narriva/tests/shared";
import { APPROVALS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetApproval", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ApprovalsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ApprovalsDDDDependencies>(
      APPROVALS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createApprovalEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetApproval, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { approvalId: entity.id };

    await getApproval(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetApproval.execute).toHaveBeenCalledWith(expect.objectContaining({ approvalId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { approvalId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetApproval, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getApproval(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
