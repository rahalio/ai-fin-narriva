/**
 * CancelDelivery handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: deliveries
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { cancelDelivery } from "@narriva/api-server/domains/deliveries/handlers";
import type { DeliveriesDDDDependencies } from "@narriva/api-server/domains/deliveries";
import { createUpdateCancelRequest } from "@narriva/factories/deliveries";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@narriva/tests/shared";
import { DELIVERIES_USE_CASES } from "../__factories__/use-cases.js";

describe("CancelDelivery", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: DeliveriesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<DeliveriesDDDDependencies>(
      DELIVERIES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = { id: 'mock-id', orgId } as any;
    const input = createUpdateCancelRequest();
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { deliveryId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCancelDelivery, "execute").mockResolvedValue(useCaseResult);

    await cancelDelivery(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCancelDelivery.execute).toHaveBeenCalledWith(expect.objectContaining({ deliveryId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
