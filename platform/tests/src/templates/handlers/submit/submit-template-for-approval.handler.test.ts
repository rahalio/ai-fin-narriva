/**
 * SubmitTemplateForApproval handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: templates
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { submitTemplateForApproval } from "@narriva/api-server/domains/templates/handlers";
import type { TemplatesDDDDependencies } from "@narriva/api-server/domains/templates";
import { createCreateSubmitRequest } from "@narriva/factories/templates";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@narriva/tests/shared";
import { TEMPLATES_USE_CASES } from "../__factories__/use-cases.js";

describe("SubmitTemplateForApproval", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TemplatesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TemplatesDDDDependencies>(
      TEMPLATES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = createCreateSubmitRequest();
    const entity = { id: 'mock-id', orgId } as any;
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeSubmitTemplateForApproval, "execute").mockResolvedValue(useCaseResult);

    await submitTemplateForApproval(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeSubmitTemplateForApproval.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
